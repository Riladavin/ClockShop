<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://stgpostgres441-1z26.yc.o3.ru:6532/sq-toloka-loader</property>
        <property name="connection.username">sq-toloka-loader-user</property>
        <property name="connection.password">lee7eepaegh1ouL</property>
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">2</property>
        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>
        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">validate</property>

        <mapping class="clock.ClockShop"/>
        <mapping class="clock.Clock"/>
        <mapping class="clock.ClocksForShops"/>
    </session-factory>
</hibernate-configuration>
